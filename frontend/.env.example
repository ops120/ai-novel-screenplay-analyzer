# 前端环境变量示例 — 复制为 .env.local 后按需修改
# Vite 约定：VITE_ 前缀的变量才能被 import.meta.env 读取

# 后端 API base URL，默认 http://127.0.0.1:28000/api
# 后端默认端口 28000 (= 8000 + 20000)，避开常用 dev 端口段
# VITE_API_BASE=http://127.0.0.1:28000/api