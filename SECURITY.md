# 安全说明

## 报告漏洞

如果你发现安全漏洞（例如 API Key 泄露、注入、越权访问或数据泄露），**不要**在公开 Issue 中提交。请通过以下方式私下联系维护者：

- 邮件：在仓库首页或 GitHub Profile 中查找维护者邮箱
- 或直接给维护者发送 GitHub 私信

请包含：

- 漏洞类型与影响范围
- 复现步骤（尽量最小化）
- 受影响的版本/提交
- 可选的修复建议

我们会尽快确认并回复。

## 安全基线

本项目遵循以下默认约定：

- 所有 API Key 只保存在本机（SQLite 加密存储或浏览器本地），不写入 README、源码或版本库。
- 数据库文件（`storymap.db`）、日志、构建产物、截图与本地密钥均已加入 `.gitignore`。
- 后端默认监听 `127.0.0.1:28000`，仅本机访问；对外暴露时请通过反向代理并自行控制访问权限。
- 部署时请使用当前版本的最新依赖，并定期更新。

## 支持范围

我们为最新发布版本提供安全修复。旧版本请尽快升级到最新版。